services:
  - type: web
    name: glopixs-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run api
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: GLOPIXS_USE_GENERATED_MEDIA
        value: '1'

